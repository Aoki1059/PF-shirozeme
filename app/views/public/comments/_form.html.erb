<%= form_with(model:[post, comment], local: false) do |f| %>
  <%= f.text_area :comment, rows:'5',placeholder: "コメントをここに", class: "form-control", id: "comment__form--input" %>
  <%= f.submit "コメントする", class: "btn btn-lg btn-base-1 mt-20 pull-right" %>
<% end %>